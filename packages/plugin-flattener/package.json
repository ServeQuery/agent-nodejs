{
  "name": "@servequery/plugin-flattener",
  "version": "1.1.0",
  "description": "A plugin that allows to flatten columns and relations in Serve Query",
  "main": "dist/index.js",
  "license": "GPL-3.0",
  "files": [
    "dist/**/*.js",
    "dist/**/*.d.ts"
  ],
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ServeQuery/agent-nodejs.git",
    "directory": "packages/plugin-flattener"
  },
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "clean": "rm -rf coverage dist",
    "lint": "eslint src test",
    "test": "jest"
  },
  "devDependencies": {
    "@servequery/datasource-customizer": "1.43.6",
    "@types/object-hash": "^3.0.2"
  },
  "dependencies": {
    "@servequery/datasource-toolkit": "1.32.2",
    "object-hash": "^3.0.0"
  }
}
